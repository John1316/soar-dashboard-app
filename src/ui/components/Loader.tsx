import { Spinner } from '@nextui-org/react'

export default function Loader() {
  return (
    <div className="flex items-center justify-center min-h-screen">
        <Spinner size="lg" color="default" /> {/* Replace with any loader component */}
      </div>
)
}
